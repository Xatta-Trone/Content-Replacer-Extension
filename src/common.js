const commonData = {
  popupStyle: `{
        display: none;
        position: absolute;
        background-color: #fff;
        border: 1px solid #ccc;
        padding: 15px;
        box-shadow: 0 2px 5px rgba(0, 0, 0, 0.2);
        z-index: 99999999;
        background: #2D8DFF;
        color: white;
        text-align: center;
        border: none;
        border-radius: 10px;
      }`,
  dropAreaStyle: `{
          border: 2px dashed #ccc;
          padding: 20px;
          text-align: center;
          cursor: pointer;
          max-width: 160px;
          margin: 0 auto;
      }`,
  dropAreaHighLightStyle: `{
          border-color: white;
      }`,
};
export default commonData;
